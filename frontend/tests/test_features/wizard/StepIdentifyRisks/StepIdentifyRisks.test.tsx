import { describe, it, expect } from "vitest";

describe.skip("StepIdentifyRisks (placeholder)", () => {
  it("renders risk identification step", () => {
    expect(true).toBe(true);
  });

  it("allows adding risks to the register", () => {
    expect(true).toBe(true);
  });
});
